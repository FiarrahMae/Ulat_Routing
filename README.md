# Ulat_Routing
The objective for this activity is to create two routes. The first route will simply return a message like 'Hello, Laravel!', the second route will call a method in your controller (GreetController) which returns a Blade view (greet.blade.php). Once you are done, submit a link to your GitHub repository.
